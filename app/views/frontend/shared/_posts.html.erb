
<div class="blog-posts">
  <% posts.each do |post| %>
    <div class="blog-post">
      <div class="href_name post-title">
        <%= link_to post.title, frontend_post_path(post), title: post.title %>
      </div>

      <p class="post-meta">
        <%= post.published_time %>
        <%= post_tags_from_str(post.tags) %>
      </p>

      <div class="post-content">
        <%= omited_str(strip_tags(post.content), 240) %>
      </div>
    </div>
  <% end %>

  <div class="pager">
    <%= paginate posts %>
  </div>
</div>
